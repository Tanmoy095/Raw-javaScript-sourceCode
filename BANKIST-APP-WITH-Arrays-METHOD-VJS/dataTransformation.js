//map
/*map is similar to foreach method but map creates a brand new araay based on the original array
map takes an array loop over the array and each itteration it applies a callback function the we specify in our code to the current array element

for example we said it will multiply by 2 each element multiplied by 2 and puts it into a new array
*/
//filter
//filter returns a new array containing the new array element that passed a specified array condition


//reduce

